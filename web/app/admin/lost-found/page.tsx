import { LostFoundWorkspace } from "@/components/admin/lost-found";

export const metadata = {
  title: "Admin Lost & Found | CampusLink",
  description: "CampusLink lost and found moderation workspace",
};

export default function AdminLostFoundPage() {
  return <LostFoundWorkspace />;
}
